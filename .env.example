# LocationIQ API key — https://locationiq.com
# Routing uses LocationIQ Directions API when this key is set.
# Without it, the app falls back to the public OSRM server (no walking profile).
VITE_LOCATIONIQ_KEY=pk.your_key_here
